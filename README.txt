the assignment cointains two file peer.cpp and tracker.cpp
each file takes the port number as input inside the program not as command line argument.
the commands which are implemented by the peer is 
   a) create_user
   b) loggin_user
   c) logout
   d) upload
   e) list_file
   d) download_file
   e) stop_sharing

NOTE:: no need to give any arguments to these commands the program will ask for the required field by itself.
NOTE:: group are not implimented but a try for groups is also kept in the extra folder.
